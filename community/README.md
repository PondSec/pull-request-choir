# Community Voices

Each file in this directory is one permanent voice in Pull Request Choir.

The generator reads these JSON files, validates them, places them into the constellation, and writes the static data used by the app.

Create yours with:

```bash
npm run new-voice
```

Then run:

```bash
npm run validate
npm run build
```
